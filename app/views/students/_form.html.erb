<%= form_for([@student]) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_name %><br>
    <%= f.text_field :student_name %>
  </div>
  <div class="field">
    <%= f.label :class_mapping %><br>
    <%= f.select :class_mapping,  options_for_select(ClassMapping.all.map{|c|[c.standard.standard_name, c.id]}) %>
    <%= f.select :class_mapping,  options_for_select(ClassMapping.all.map{|c|[c.section.section_name, c.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
