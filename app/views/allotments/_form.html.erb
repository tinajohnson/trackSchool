<%= form_for(@allotment) do |f| %>
  <% if @allotment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allotment.errors.count, "error") %> prohibited this allotment from being saved:</h2>

      <ul>
      <% @allotment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="field">
    <%= f.label :school_id %><br>
    <%= f.number_field :school_id %>
  </div>
  <div class="field">
    <%= f.label :class_mappings_id %><br>
    <%= f.number_field :class_mappings_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
