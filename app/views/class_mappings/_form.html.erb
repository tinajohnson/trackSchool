<%= form_for(@class_mapping) do |f| %>
  <% if @class_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_mapping.errors.count, "error") %> prohibited this class_mapping from being saved:</h2>

      <ul>
      <% @class_mapping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%
    s_id = Allotment.where(user_id: current_user.id).pluck(:school_id)
  %>

  <div class="field">
    <%= f.label :standard_id %><br>
    <%= f.select :standard_id,  options_for_select(Standard.all.map{|s|[s.name, s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :section_id %><br>
    <%= f.select :section_id,  options_for_select(Section.all.map{|s|[s.name, s.id]}) %>
  </div>
  <%= hidden_field_tag :school_id, s_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
