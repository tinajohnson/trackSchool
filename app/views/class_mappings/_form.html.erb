<%= form_for(@class_mapping) do |f| %>
  <% if @class_mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_mapping.errors.count, "error") %> prohibited this class_mapping from being saved:</h2>

      <ul>
      <% @class_mapping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :standard_id %><br>
      <%= f.select :standard_id,  options_for_select(Standard.all.map{|s|[s.standard_name, s.id]}) %>
    </div>
    <div class="field">
      <%= f.label :section_id %><br>
      <%= f.select :section_id,  options_for_select(Section.all.map{|s|[s.section_name, s.id]}) %>
    </div>
    <div class="field">
      <%= f.label :school_id %><br>
      <%= f.select :school_id,  options_for_select(School.all.map{|s|[s.school_name, s.id]}) %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
